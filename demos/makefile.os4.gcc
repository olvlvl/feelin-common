# create all executables

OBJ := Alpha Class1 Class2 \
	ColorCube CrazyGauges Dialog DnD fonts \
	FWidgetPublic Gradient Lines Listview \
	SignalHandler Strings Thread XMLApplication
#ColorCube24 Gradient


all : $(addsuffix _OS4,$(OBJ))

%_OS4 : %.c
	@ echo "[2mCompiling $@...[0m"
	gcc -IFeelin:Include -D__USE_BASETYPE__ -mcrt=clib2 -DF_USE_PICASSO96 -LFeelin:Include/lib -o $@ $< -lauto
	@ echo "[1m$< compiled.[0m"

clean :

	@ Delete QUIET #?.(o|lnk)

delete :

	@ Delete QUIET #?_OS4

